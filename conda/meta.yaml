package:
  name: elastic
  version: !!str 4.0.7

source:
  git_url: git@github.com:jochym/elastic.git
#  git_tag: 4.0
#  fn: elastic-4.0.5.tar.gz
#  url: https://pypi.python.org/packages/source/e/elastic/elastic-4.0.5.tar.gz
#  md5: 6ebb277694e04f56644776d4396b3933
#  patches:
   # List any patch files here
   # - fix.patch

build:
  #preserve_egg_dir: True
  #entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elastic = elastic:main
    #
    # Would create an entry point called elastic that calls elastic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 0

requirements:
  build:
    - python

  run:
    - python
    - python-ase
    - numpy
    - scipy
    - spglib

test:
  # Python imports
  imports:
    - elastic

  #commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jochym/Elastic
  license: GPLv3
  summary: 'Extension for ASE to calculate elastic constants'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
